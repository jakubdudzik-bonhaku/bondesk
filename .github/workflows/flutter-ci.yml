name: Full Flutter CI

# BonDesk: only manual trigger. Real builds use bondesk-build.yml.
on:
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
